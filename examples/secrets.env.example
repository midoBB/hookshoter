# Hookshot Secrets File Example
# Copy to /etc/hookshot/secrets and fill in your values
#
# Format: KEY=VALUE (one per line)
# Comments start with #
#
# Security: This file should be readable only by the hookshot user
#   chmod 400 /etc/hookshot/secrets
#   chown hookshot:hookshot /etc/hookshot/secrets

# HMAC key for webhook authentication (REQUIRED)
# Generate with: openssl rand -hex 32
hmac_key=YOUR_HMAC_KEY_HERE

# Slack webhook URL for failure notifications (optional)
slack_webhook_failure=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Slack webhook URL for rollback notifications (optional)
slack_webhook_rollback=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# Discord webhook URL (optional)
discord_webhook=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Database encryption key (future use)
# db_encryption_key=YOUR_ENCRYPTION_KEY_HERE

# External API keys (if needed by deployment scripts)
# registry_api_key=YOUR_REGISTRY_API_KEY
# cloud_api_key=YOUR_CLOUD_API_KEY